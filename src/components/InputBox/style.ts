import tw from 'twin.macro';

export const Label = tw.label`block mb-2 text-sm font-medium text-gray-900`;
export const IconContainer = tw.div`absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none`;
export const Input = tw.input`bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full pl-10 p-2.5`;
